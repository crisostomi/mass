import copy

from mass.modules.encoder import ImageEncoder


class TaskVectorBasedMerger:

    def __init__(self):
        super().__init__()

    def merge(self, base_model, finetuned_models) -> ImageEncoder | None:

        pass
